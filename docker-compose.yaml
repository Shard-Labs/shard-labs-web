version: "3.3"
services: 
  shard-labs-web:
    image: shardlabs/shard-labs-web:latest
    container_name: shard-labs-web
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
    - "5000:5000"
    restart: always
